# Hướng dẫn chạy project

## Cài đặt thư viện

Trước khi chạy project, bạn cần cài đặt một số thư viện cần thiết.
### virtual enviroment
```bash
python venv -m myenv
```

### pandas
```bash
pip install pandas
```

